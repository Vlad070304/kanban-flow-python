name: Type Checking and Linting

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  quality:
    name: Tests, mypy, and Ruff
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install development tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run Ruff
        run: python -m ruff check .

      - name: Check Ruff formatting
        run: python -m ruff format --check .

      - name: Run mypy
        run: python -m mypy

      - name: Run unit tests
        run: python -m coverage run --source=models,services -m unittest discover -s tests -p "test_*.py" -v

      - name: Report test coverage
        run: python -m coverage report --show-missing --fail-under=75

      - name: Generate coverage artifact
        run: python -m coverage xml

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
